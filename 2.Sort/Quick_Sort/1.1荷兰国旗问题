
时间复杂度O(N)，空间复杂度O(1),不具有稳定性


荷兰国旗问题和partition最大的不同：
	荷兰国旗：A[i]与A[L]和A[R]交换
	partition：A[L]与A[R]交换

#include<iostream>
using namespace std;
void holanflag(int A[], int N){
	int begin = 0, end = N - 1;
	for (int i = 0; i <= end;){
		switch (A[i]){
			case 1:
				i++;  
				break; 
			case 0: 
				swap(A[begin++], A[i++]); 
				break;
			case 2: 
				swap(A[i], A[end--]);
		}
	}
}
void test(){
	int A[] = { 0,0,2,2,1,1,2,0,1 };
	int len = sizeof(A) / sizeof(A[0]);
	holanflag(A,len);  //荷兰国旗
}

int main(){
	test();
	system("pause");
}
