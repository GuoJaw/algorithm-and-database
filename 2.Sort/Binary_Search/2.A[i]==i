
有一个有序数组arr，其中不含有重复元素，请找到满足arr[i] == i条件的位置

int findAi_i(int A[], int N){
	int ret = -1;

	int l = 0, r = N - 1;
	while (l <= r){
		int mid = l + (r - l) / 2;
		if (A[mid] == mid){
			ret = mid;  //记录相等位置
			r = mid - 1;
		}
		else if (A[mid] > mid)
			r = mid - 1;
		else
			l = mid + 1;
	}
	return ret;
}

