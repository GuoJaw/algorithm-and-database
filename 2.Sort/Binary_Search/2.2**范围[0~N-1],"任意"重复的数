[剑指offer]在一个长度为N的数组中，所有数[都在0~N-1范围]内。数组中某些数字是重复的，但不知道有几个
数字重复，也不知道每个数字重复了几次, 找出"任意"出现的重复的数

if A[i]==i:
    i++;
else if A[i]==A[A[i]]
    return A[i];
else
    swap(A[i],A[A[i]]);  //一直交换，直到换出一个符合A[i]==i的情况

