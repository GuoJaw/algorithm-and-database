 案例
    [ 1   5   4   2   6   7 ]
 答案:
    [ 5   4   2   6 ]
    返回值：len = 4    

 最优解的时间复杂度O(N)，空间复杂度O(1)
 
int getMinSort(int A[], int N){
	int l;
	for (l = 0; l < N && l + 1 < N && A[l] < A[l + 1]; l++);
	if (l + 1 >= N)
		return 0;
	int r;
	for (r = N - 1; r >0 && r - 1 > 0 && A[r] > A[r - 1]; r--);
	return r - l + 1;
}
int main(){
	int A[] = { 3,2,1,0 };
	int N = sizeof(A) / sizeof(A[0]);
	int ret = getMinSort(A, N);
}
