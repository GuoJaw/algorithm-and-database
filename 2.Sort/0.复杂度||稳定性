(1)时间复杂度
    O(N)   桶排序（计数排序|基数排序）——不是基于比较的排序
    O(N*N) 冒泡 插入 选择
    O(N*lgN) 归并排序 快速排序 希尔排序 堆排序 
(2)空间复杂度
    O(1)  插入/选择/冒泡/堆排序/希尔
    O(lgN)~O(N)  快速排序   取决于划分的情况
    O(N)  归并排序
    O(M)  桶排序  
(3)不稳定:
    选择  快速  希尔  堆排序
  
稳定性的优点：原始排好序的信息，会保留下来
      班级  分数 
    张  2   17
    王  1   20
    李  1   60
    周  2   60
分析:
    先按照"分数"排序
    再按照"班级"排序：如果稳定，那么按照每个班级排好序的结果，分数也是有序的。

(4)综合排序的选择方案（面试）
        数据量小于60时：
            插排：虽然时间复杂度大，常数项低
        数据量大于60时：从稳定性出发
            快排：基础类型
            归并：struct student

