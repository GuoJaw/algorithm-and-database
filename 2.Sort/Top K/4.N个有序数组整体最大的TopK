打印N个有序数组整体最大的TopK
例如，输入含有N行元素的二维数组可以代表N个一维数组
219，405，538，845，971
148，558
52，99，348，691
再输入整数k=5，则打印：
Top5: 971，845，691，558，538

分析：注意，每个数组都是有序的
（1）拿每个数组的最后一个元素建立大小为N的大根堆
（2）建好之后，此时的堆顶元素就是所有数组元素的最大值，打印堆顶元素
（3）假设堆顶元素来自a数组的i位置，那么接下来就把堆顶元素的前一个数（即A[i-1]）替换原来的堆顶，然后AdjustDown，使之重新变成大根堆
（4）这样每次可以得到一个堆顶元素max，在打印完成后都要经历步骤（3）的调整过程
（5）整体打印K次，就是从大到小全部的TopK
注意：在重复（3）的过程中，有可能max来自的那个数组（仍假设是a数组）已经没有元素，也就是说max是a[0]，再往左
没有数了。======>那么就把堆中的最后一个元素放在堆顶位置，然后把堆的大小减一，最后依然是从堆的头部开始AdjustDown，使其重新变成大根堆
