priority_queue

堆排序: O(N*lgK)    
（1）首先拿前K个数建立大根堆
（2）接下来遍历整个数组，遍历过程中看当前数是否比堆顶元素小，
        [1]如果A[i]小于堆顶元素，则用A[i]替换堆顶元素B[0]，然后AdjustDown(B, 0, K);
        [2]如果A[i]不小于堆顶元素，则i++
（3）直到i==N-1，遍历完成后，此时堆中的元素，即为所有数组中最小的K个数
