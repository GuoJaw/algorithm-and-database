双指针：i  j

只有赋值的时候，i才++，否则只是j++

int remove(vector<int>& vec){
	int i = 0;
	for (int j = 1; j < vec.size();){
		if (vec[i] == vec[j])
			j++;
		else
			vec[++i] = vec[j];
	}
	return i + 1;
}
int main(){
	vector<int> vec = { 1, 1, 2, 3, 4, 4, 5, 7, 8, 8, 9 };
	int ret = remove(vec);
	for (int i = 0; i < ret; i++)
		cout << vec[i] << "  ";
}
