
void delspace(char *str){
	int index = 0;
	for (int i = 0; i < strlen(str); i++){ //for (int i = 0; str[i]!='\0'; i++)
		if (str[i] != ' ')
			str[index++] = str[i];
	}
	str[index] = '\0';
}
int main(){
	char str[] = "33  33";
	delspace(str);
}
