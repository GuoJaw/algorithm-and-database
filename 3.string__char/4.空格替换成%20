1.假设str有足够的空间，将其中所有的空格替换成%20
答案：
  先遍历一遍str，找到空格的个数K---->str在替换后，长度为str.length+2*K
  使用两个索引i和j，i指向str的末尾，j指向第str.length+2*K个位置，i和j都从后向前遍历
      如果str[i]!=' '--->str[j]=str[i]
      如果str[i]==' '--->str[j]，str[j-1]，str[j-2]等于02%


2.字符串匹配问题
给定一个字符串str，判断是不是整体有效的括号字符串
举例：
  (),true
  ()),false  
  ()a(),false
最优解：时间复杂度O(N),空间复杂度O(1)
答案：
  1.整形变量num，代表在遍历过程中，(出现次数和)出现次数的差值
  2.遍历过程中，遇到(，则num++
  3.遍历过程中，遇到)，则num--
  4.遍历过程中，如果num<0，则说明)多余(，直接返回false
  5.如果一直没有出现情况4，则一直遍历下去
  6.遍历完成后，如果num==0，返回true，否则返回false
