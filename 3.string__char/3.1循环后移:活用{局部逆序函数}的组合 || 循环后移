====================
【活用{局部逆序函数}的组合】void reverse(string str,int l,int r);
给定str和i，i代表str中的位置
要求：将str[0,i-1]移动到右侧，str[i+1,N-1]移动到左侧
    时间O(N)，空间O(1)
举例：str="ABCDE",i=2----->CDEAB
分析：由于空间O(1)，因此不能使用额外的char数组保存--->必须进行原地交换
    [0,i-1]局部逆序:BA CDE
    [i,N-1]局部逆序：BA EDC
    整体逆序---> CDEAB
void swap(int &a, int &b){
	a ^= b; b ^= a; a ^= b;
}
//[l,r]范围内的string反转
void reverse(string& str,int l,int r){
	while (l < r){
		swap(str[l++], str[r--]);
	}
}
void Reverse_I(string& str, int i){
	if (str == "" || i <= 0)
		return;
	reverse(str, 0, i - 1); //[0, i - 1]局部逆序:BA CDE
	reverse(str, i, str.length()-1); //[i,N-1]局部逆序：BA EDC
	reverse(str, 0, str.length()-1); //整体逆序---> CDEAB
}


