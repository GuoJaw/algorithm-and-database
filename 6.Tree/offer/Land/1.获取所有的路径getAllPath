
void getAllPath(TreeNode* T, vector<TreeNode*> path){
	if (!T)
		return;
	path.push_back(T);
	if (!T->left && !T->right)
		//打印path中所有的元素，即为一条路径
	if (T->left)
		getAllPath(T->left, path);
	if (T->right)
		getAllPath(T->right, path);
	path.pop_back();
}

