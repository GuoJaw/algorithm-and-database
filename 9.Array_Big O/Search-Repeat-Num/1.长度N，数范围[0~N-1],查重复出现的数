

[剑指offer]在一个长度为N的数组中，所有数都在0~N-1范围内。数组中某些数字是重复的，但不知道有几个
数字重复，也不知道每个数字重复了几次, 找出"任意"出现的重复的数
分析：限定了范围——>所有数都在0~N-1范围内

最优条件下，应该满足A[i]=i

解：核心解法——>使用swap
	将A[i]!=i && A[A[i]]!=A[i]的情况，进行交换swap(A[A[i]],A[i]);
	直到交换成A[i]==i为止，才i++

int getNumber(int A[], int N){
	if (A == NULL || N <= 0)
		return -1;
	for (int i = 0; i < N;){
		if (A[i] == i) //相等，就i++
			i++;
		else{ //不相等，此时i位置是A[i]
			if (A[i] == A[A[i]]) //判断A[i]位置是不是也是A[i]，如果是，重复
				return A[i]; //直接返回
			else //如果A[i]位置不是A[i]
				swap(A[i], A[A[i]]); //进行交换
		}
	}
	return -1;
}
=======================

【题目进阶】：要求不能使用swap！（换句话说，不能改变数组）
