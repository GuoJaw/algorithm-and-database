题目1：判断一个N位的数是+还是-？
    一个N位数的二进制是+/-，取决于该数的二进制的第一位是不是1：如果是1，为-；是0，为+
        (N的二进制位的第一位 & 1)，如果：
            等于1，是负数
            等于0，是非负数
        代码：(N>>(N-1))&1   //将N向右移动N-1位，再&1
        
int flip(int N){
  return N^1; //^1表示二进制取反
}
int sign(int N){  //获取32位整数的符号;负数返回1；不是负数返回0
  return flip((n>>31)&1);
}
int getMax(int a,int b){
  int c=a-b;
  int A=sign(c);  //c为负数，返回1，表示b大；c不是负数，返回0，表示a大
  int B=flip(A);  //
  return a*A+b*B;  //A和B一个为1，一个为0
} 
