== == == == == == == == == == =
【进阶问题】
有两个数出现了奇数次，其他数出现了偶数次，打印这两个数。
(假设两个奇数次的数是a和b)
解：
(1)整个数组中的元素，相互异或----->a^b
(2)找到a^b中第一个位==1的位
(3)重新便利数组，根据该为是否==1，分成两个字数组

void printOddTimesNum2(vector<int> arr)
{
	int e1 = 0;
	int e2 = 0;
	for (int i = 0; i<arr.size(); i++)
	{
		e1 ^= arr[i];
	}
	int rightOne = e1 & (~e1 + 1);
	for (int i = 0; i<arr.size(); i++)
	{
		if ((arr[i] & rightOne) != 0)
		{
			e2 ^= arr[i];
		}
	}
	cout << e2 << " " << (e2^e1) << endl;
}

