^异或运算
    不同才为真（1），否则为假（0）
    例如两个2进制数异或
    10110100
    01101010
 --------------
    11011110

(1)判断32位的整数a，是不是负数？
    return (a>>31)&1; //负数，返回1；非负数，返回0

(2)一个数的二进制是+/-，取决于该数的二进制的第一位是不是1：如果是1，为-；是0，为+
(N的二进制位的第一位 & 1)，如果：
    等于1，是负数
    等于0，是非负数
代码：(N>>(N-1))&1   //将N向右移动N-1位，再&1
